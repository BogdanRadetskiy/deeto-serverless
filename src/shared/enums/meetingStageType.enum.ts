export enum MeetingStageTypeEnum {
  INVITED = 'invited',
  SELECTED = 'selected',
  ACCEPTED = 'accepted',
  SCHEDULED = 'scheduled',
  CONFIRMED = 'confirmed',
  LIVE = 'live',
  RESCHEDULED = 'rescheduled',
  COMPLETED = 'completed',
}
